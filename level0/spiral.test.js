import { spiral } from "./spiral";

describe("I로 만들기", () => {
  test("테스트1--", () => {
    expect(spiral(4)).toEqual(
      [1, 2, 3, 4],
      [12, 13, 14, 5],
      [11, 16, 15, 6],
      [10, 9, 8, 7]
    );
  });

  test("테스트1--", () => {
    expect(spiral(5)).toEqual(
      [1, 2, 3, 4, 5],
      [16, 17, 18, 19, 6],
      [15, 24, 25, 20, 7],
      [14, 23, 22, 21, 8],
      [13, 12, 11, 10, 9]
    );
  });
});
